baseURL = 'https://nova.spees.dev/'
languageCode = 'en-us'
title = 'nova spees'

# disable unused Hugo features
disableKinds = ['taxonomy', 'term', 'section']

[params]
  description = 'Nova Spees is a five-year-old pitbull terrier.'
  author = 'Team Nova'
  authorUrl = "https://github.com/shelbyspees/nova"

[markup.goldmark.renderer]
  # required to render embedded html in content pages
  # like the embedded twitter timeline
  unsafe = true 
